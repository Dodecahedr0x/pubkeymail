# PubKeyMail Agents

Start with [`.agents/README.md`](.agents/README.md). Load only the context,
memory, spec, and skill relevant to the task; archived history is opt-in. Update
the affected `.agents` file whenever implementation makes it stale.
